safety:
  force_halt: false
  force_halt_reason: "manual_halt"

feed_mode: csv_replay

csv_replay:
  path: "data/replay/market_features_sample.csv"
  max_rows: 2000

nt8_live:
  transport: "named_pipe"
  pipe_name: "URIAH_TRADING_FEED"

volatility:
  threshold_ok: 0.80
  threshold_block: 1.20

hmm:
  enabled: true
  stub_mode: "alternating"

early_exit:
  force_active: false
  force_permission: "REDUCE"
  force_reason: "manual_early_exit"